<div class="details">
    <img src="{{recipe.imageUrl}}" alt="" class="recipe-image">
    <div class="recipe-info">
        <h2>Title: {{recipe.title}}</h2>
        <aside class="ingredients">
            <h2>Ingredients:</h2>
            <ul>
                {{#each ingredients}}
                <li>{{this}}</li>
                {{/each}}   
            </ul>
        </aside>
        <h2>Instructions:</h2>
        <p>{{recipe.instructions}}</p>
        {{#if isOwner}}
            <a href="/recipes/{{recipe._id}}/edit" class="edit-btn">Edit</a>
            <a href="/recipes/{{recipe._id}}/delete" class="delete-btn">Delete</a>
        {{/if}}
    </div>
</div>